<snippet>
    <content><![CDATA[
// @flow
import * as React from 'react';
import { compose } from 'recompose';

type PropsT = {
  ${2:prop}: ${3:string},
};

const ${1:${TM_FILENAME/(.?\w*)(?:\.\w+)*$/$1/g}} = ({ ${2:prop} }: PropsT) => {
  return (
    ${4:<div />}
  );
};

export default compose(
)(${1:${TM_FILENAME/(.?\w*)(?:\.\w+)*$/$1/g}});

]]></content>
    <tabTrigger>recomp</tabTrigger>
    <scope>source.js</scope>
    <description>Recompose: createComponent</description>
</snippet>
